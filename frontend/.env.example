# Frontend Environment Variables

# API Base URL (relative path for Docker, absolute for local dev)
# For Docker: use relative path
REACT_APP_API_URL=/api/v1

# WebSocket URL (relative path for Docker, absolute for local dev)
# For Docker: use relative path
REACT_APP_WS_URL=/ws

# For local development (without Docker):
# REACT_APP_API_URL=http://localhost:8000/api/v1
# REACT_APP_WS_URL=ws://localhost:8000/ws

# Polling for file changes in Docker
CHOKIDAR_USEPOLLING=true

# WebSocket port for dev server
WDS_SOCKET_PORT=0
